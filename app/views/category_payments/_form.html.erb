<%= form_with(model: category_payment) do |form| %>
  <% if category_payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_payment.errors.count, "error") %> prohibited this category_payment from being saved:</h2>

      <ul>
        <% category_payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :payment_id, style: "display: block" %>
    <%= form.text_field :payment_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
